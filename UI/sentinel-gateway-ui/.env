# API Gateway base URL (all frontend requests route through the gateway)
VITE_API_BASE_URL=http://localhost:8080/api

# Feature flag: set to "true" to use MSW mocks instead of real backend
VITE_USE_MOCKS=false