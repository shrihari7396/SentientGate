# Development: use Vite proxy (requests go to /api/* which proxy forwards to gateway)
VITE_API_BASE_URL=/api

# Set to "true" to fall back to MSW mock data (useful when backend is not running)
VITE_USE_MOCKS=false
